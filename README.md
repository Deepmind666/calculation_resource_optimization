# calculation_resource_optimization

本项目是“本地计算资源动态调度与防爆保护”系统：
1. 实时监控 CPU/GPU/内存；
2. 动态调度任务进程；
3. 在高水位时限流与紧急回收，降低卡死/重启风险。

## 目录
1. `spec/`：架构、数据模型、伪代码、入门说明。
2. `prototype/`：可运行原型与测试。
3. `figures/`：实验指标输出（CSV/JSON）。
4. `qa/`：自查脚本与评审清单。
5. `logs/`：带时间戳的进展日志。

## 快速开始
见 `RUNBOOK.md`。
