# 附图文字描述稿（CNIPA，R43）

- 时间戳：2026-02-14 18:17:31 +08:00
- 说明：本稿用于绘图与代理协作，分为“正式附图说明句”和“绘图文字描述”两部分。

## 0. 图面状态说明（提交前必看）

1. 当前图1至图4属于实验阶段附图，图面文字和标号体系尚未完全提交态。
2. 在正式提交 CNIPA 前，需完成图面中文化、参考编号补齐以及图文一致性复核。
3. 图内编号必须与附图标记说明表一一对应，避免出现“图面标号缺失”或“图文标号不一致”。
4. 图3和图4如存在英文图例或英文坐标轴，需替换为中文并与说明书术语统一。

## 一、正式附图说明句（可直接放入说明书）

图1为本发明实施例中多资源动态调度系统模块结构示意图。  
图2为本发明实施例中单个调度周期流程示意图。  
图3为本发明实施例中三种调度模式真机对比示意图。  
图4为本发明实施例中自适应调优轨迹示意图。

## 二、绘图文字描述（给绘图人员）

### 图1 系统模块结构图（建议横向闭环布局）

1. 画布中心标注“100 多资源动态调度系统”。  
2. 左侧输入区域放置“110 资源监控模块”，下方连接“200 资源快照”，并分出“210 原始资源快照”和“220 平滑资源快照”。  
3. 中上区域放置“120 模式判定模块”，由 210、220 双输入连接。  
4. 中左区域放置“130 准入控制模块”，其输入来自模式判定模块和“300 待执行任务队列”。  
5. 中右区域放置“140 定向抢占模块”，其输入来自模式判定模块和“500 抢占候选任务集合”。  
6. 下中区域放置“150 任务执行模块”，接受准入控制模块与定向抢占模块输出。  
7. 右侧放置“160 事件审计模块”，接收 120、130、140、150 的事件输出。  
8. 下右侧放置“170 参数调优模块”，输入来自 160，输出反馈到 120、130、140。  
9. 若版面允许，在底部放置“180 资源画像模块”，输入来自 150，输出反馈到 130。  
10. 用闭环箭头体现“监控 -> 判定 -> 准入/回收 -> 执行 -> 审计 -> 调优 -> 判定”的反馈路径。

### 图2 调度周期流程图（建议纵向流程 + 分支）

1. 起点框：`S101 采集资源状态并形成原始资源快照`。  
2. 第二框：`S102 更新平滑资源快照并执行双视图模式判定`。  
3. 判定菱形：`是否紧急模式`。  
4. 非紧急分支：  
   - `S103 同周期累计资源投影准入判定`；  
   - `S104 准入成功后更新后续候选任务投影基线`。  
5. 紧急分支：  
   - `S105 阻断新任务准入`；  
   - `S106 识别瓶颈资源维度与热点图形处理器`；  
   - `S107 计算归一化回收评分并执行定向抢占`；  
   - `S108 判断双目标停止条件是否满足`。  
6. 汇合框：`S109 刷新任务状态并记录审计事件`。  
7. 结束框：`S110 输出本周期调度结果并进入下一周期`。  
8. 在 S103 附注“按卡分桶预算 + 未绑定保守预算”。  
9. 在 S107 附注“可启用协同增强项与亲和加权”。  
10. 在 S108 附注“内存目标与图形处理器目标同时满足才停止”。

### 图3 真机三模式对比图（建议柱状+折线组合）

1. 横轴设置三组模式：无调度、固定并发、动态调度。  
2. 柱状指标至少包含：内存峰值占比、图形处理器显存峰值占比。  
3. 折线指标至少包含：任务完成数、阻断次数。  
4. 对动态调度组加注释“触发紧急模式并执行定向回收”。  
5. 对无调度组加注释“高峰风险基线”。  
6. 对固定并发组加注释“无资源感知回收”。  
7. 标注实验环境说明框（可放右上角）。  
8. 图例需清楚区分内存、显存、完成数、阻断数。  
9. 数据来源指向 `figures/` 中对应 CSV/JSON。  
10. 图注建议说明“本图用于展示安全与吞吐的平衡效果”。

### 图4 自适应调优轨迹图（建议阶段流程图）

1. 横向按轮次画出 Attempt-1、Attempt-2、Attempt-3（如有）。  
2. 每轮分四个节点：输入信号、重试原因、调优动作、输出结果。  
3. 输入信号框示例：`紧急触发次数`、`任务完成数`、`阻断次数`。  
4. 重试原因框示例：`紧急信号不足`、`吞吐不足`。  
5. 调优动作框示例：`阈值收紧`、`阈值放宽`、`负载参数调整`。  
6. 输出结果框示例：`已启动任务数`、`已完成任务数`、`抢占次数`。  
7. 使用箭头表示“按失败原因分支调节”的路径。  
8. 在图注中强调“调优是反馈闭环，不是固定脚本”。  
9. 与图1中的 170 参数调优模块保持术语一致。  
10. 图注建议说明“用于支撑参数自适应实施方式”。
